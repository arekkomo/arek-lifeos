# Dashboard Auth Setup

Placeholder consolidated reference. Use current Hermes docs as source of truth for DashboardAuthProvider registration and secure remote dashboard binds. Prefer SSH tunnels unless a proper dashboard auth provider is configured.
